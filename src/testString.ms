minespeak

func Test(param1:num) -> bool do

    do
    var n : num = 2 + 3

    while 2+2             endwhile

    /*
    for var gamer : num = 0 until gamer > 10 where gamer += 1 do
        var n : num = 2 + 3
    endfor
    var flag : num = 2
    if flag do

    endif*/
endfunc

closespeak